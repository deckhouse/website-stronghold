---
title: "Автоматические снимки"
linkTitle: "Автоматические снимки"
description: "Настройка автоматического резервного копирования встроенного хранилища Deckhouse Stronghold"
weight: 30
---

Автоматические снимки позволяют **Deckhouse Stronghold** по расписанию создавать резервные копии встроенного backend-хранилища `Raft` и сохранять их на локальный диск или в `S3`-совместимое объектное хранилище.

> Предупреждение  
> Автоматические снимки доступны только при использовании интегрированного backend-хранилища `Raft`. Для `etcd`, `postgresql` и других внешних backend-хранилищ нужно настраивать резервное копирование средствами самого backend-хранилища.

## Когда использовать

Автоматические снимки подходят для регулярного резервного копирования без ручного запуска команд. Они особенно полезны для production-кластеров, где важно иметь повторяемую политику хранения и выносить резервные копии за пределы самого кластера.

Автоматические снимки рекомендуется использовать, если необходимо:

- регулярно создавать резервные копии без участия администратора;
- сократить риск пропуска резервного копирования;
- централизованно управлять политикой хранения;
- хранить копии за пределами кластера или площадки размещения;
- строить проверяемую схему аварийного восстановления.

## Как это работает

В Stronghold можно создать несколько именованных конфигураций автоматических снимков.

Каждая конфигурация определяет:

- интервал запуска;
- политику хранения;
- тип хранилища;
- путь или префикс размещения;
- дополнительные параметры, зависящие от типа хранилища.

Поддерживаются типы хранилища:

- `local`;
- `aws-s3`.

Для production-окружений локальное хранилище обычно менее предпочтительно, чем внешнее объектное хранилище: active-узел кластера может меняться, а резервные копии лучше хранить отдельно от защищаемой системы.

## Требования и ограничения

Перед настройкой автоматических снимков убедитесь, что:

- Stronghold использует встроенное `Raft`-хранилище как основной `storage` backend;
- у вас есть права `sudo` на соответствующие системные пути;
- целевое хранилище доступно со стороны Stronghold;
- для `local`-хранилища существует директория назначения и она доступна для записи;
- для `S3`-совместимого хранилища создан бакет и выданы корректные учётные данные.

Также учитывайте:

- конфигурации автоматических снимков не заменяют проверку резервных копий;
- удаление конфигурации не удаляет уже созданные файлы снимков;
- успешное создание снимка не гарантирует успешного восстановления без отдельной проверки процедуры восстановления.

## Создание или обновление конфигурации

Для работы используется endpoint:

| Метод | Путь |
| --- | --- |
| `POST` | `/sys/storage/raft/snapshot-auto/config/:name` |

Для работы с endpoint требуются права `sudo`.

## Основные параметры

| Параметр | Тип | Обязательный | Значение по умолчанию | Описание |
| --- | --- | --- | --- | --- |
| `name` | Строка | Да | — | Имя конфигурации, которую нужно создать или обновить |
| `interval` | Целое число или строка | Да | — | Интервал между созданием резервных копий. Можно указывать в секундах или в формате Go duration, например `24h` |
| `retain` | Целое число | Нет | `3` | Сколько резервных копий хранить. При превышении лимита самые старые копии удаляются |
| `storage_type` | Неизменяемая строка | Да | — | Тип хранилища: `local` или `aws-s3` |
| `path_prefix` | Неизменяемая строка | Да | — | Для `local` это директория хранения снимков. Для `aws-s3` это префикс объекта в бакете |
| `file_prefix` | Неизменяемая строка | Нет | `stronghold-snapshot` | Префикс имени файла или объекта |

## Дополнительные параметры для local

| Параметр | Тип | Обязательный | Значение по умолчанию | Описание |
| --- | --- | --- | --- | --- |
| `local_max_space` | Целое число | Нет | `0` | Максимальный объём в байтах, который можно занять резервными копиями с указанным `file_prefix` в директории `path_prefix`. Значение `0` отключает проверку |

## Дополнительные параметры для aws-s3

| Параметр | Тип | Обязательный | Значение по умолчанию | Описание |
| --- | --- | --- | --- | --- |
| `aws_s3_bucket` | Строка | Да | — | Имя бакета для хранения резервных копий |
| `aws_s3_region` | Строка | Нет | — | Регион бакета |
| `aws_access_key_id` | Строка | Нет | — | Идентификатор ключа доступа к бакету |
| `aws_secret_access_key` | Строка | Нет | — | Секретный ключ доступа к бакету |
| `aws_s3_endpoint` | Строка | Нет | — | Endpoint `S3`-сервиса |
| `aws_s3_disable_tls` | Булевый | Нет | — | Отключает TLS для `S3`-endpoint. Используйте только для тестирования |
| `aws_s3_ca_certificate` | Строка | Нет | — | CA-сертификат для `S3`-endpoint в PEM-формате |

## Примеры конфигурации

### Локальный диск

Следующий JSON-файл `local-snapshot.json` создаёт конфигурацию, которая сохраняет снимок каждые 5 минут в каталог `/stronghold/data/backups`, хранит 4 копии и использует префикс `main_stronghold`:

```json
{
  "interval": "5m",
  "path_prefix": "/stronghold/data/backups",
  "file_prefix": "main_stronghold",
  "retain": "4",
  "storage_type": "local"
}
```

Применение конфигурации:

```bash
d8 stronghold write sys/storage/raft/snapshot-auto/config/my-local-snapshots @local-snapshot.json
```

> Примечание  
> Перед применением конфигурации убедитесь, что каталог из `path_prefix` существует и доступен для записи. Ошибка `failed to create snapshot directory at destination` обычно означает, что каталог отсутствует или недоступен.

### S3-совместимое хранилище

Следующий файл `minio-snapshot.json` сохраняет снимки в `S3`-совместимое хранилище:

```json
{
  "interval": "3m",
  "path_prefix": "snapshots",
  "file_prefix": "stronghold_backup",
  "retain": "15",
  "storage_type": "aws-s3",
  "aws_s3_bucket": "my_bucket",
  "aws_s3_endpoint": "minio.domain.ru",
  "aws_access_key_id": "<ACCESS_KEY>",
  "aws_secret_access_key": "<SECRET_ACCESS_KEY>"
}
```

Применение конфигурации:

```bash
d8 stronghold write sys/storage/raft/snapshot-auto/config/my-remote-snapshots @minio-snapshot.json
```

> Примечание  
> Перед применением конфигурации убедитесь, что бакет уже существует, а указанные учётные данные имеют права на чтение и запись.

### Обновление существующей конфигурации

Чтобы изменить только часть параметров, достаточно передать неполный JSON:

```json
{
  "interval": "3m",
  "retain": "10"
}
```

```bash
d8 stronghold write sys/storage/raft/snapshot-auto/config/my-local-snapshots @local-snapshot-update.json
```

## Просмотр списка конфигураций

Для вывода списка конфигураций используйте:

| Метод | Путь |
| --- | --- |
| `LIST` | `/sys/storage/raft/snapshot-auto/config` |

```bash
d8 stronghold list sys/storage/raft/snapshot-auto/config
```

## Получение параметров конфигурации

Для чтения конкретной конфигурации используйте:

| Метод | Путь |
| --- | --- |
| `GET` | `/sys/storage/raft/snapshot-auto/config/:name` |

```bash
d8 stronghold read sys/storage/raft/snapshot-auto/config/my-remote-snapshots
```

Для `aws-s3` значения `aws_access_key_id` и `aws_secret_access_key` в ответе не отображаются.

## Удаление конфигурации

Для удаления конфигурации используйте:

| Метод | Путь |
| --- | --- |
| `DELETE` | `/sys/storage/raft/snapshot-auto/config/:name` |

```bash
d8 stronghold delete sys/storage/raft/snapshot-auto/config/my-remote-snapshots
```

> Примечание  
> Удаление конфигурации автоматического резервного копирования не удаляет уже созданные файлы снимков из локального или объектного хранилища.

## Получение статуса

Для просмотра статуса автоматических снимков используйте:

| Метод | Путь |
| --- | --- |
| `GET` | `/sys/storage/raft/snapshot-auto/status/:name` |

```bash
d8 stronghold read sys/storage/raft/snapshot-auto/status/my-remote-snapshots
```

Ключевые поля статуса:

- `consecutive_errors` — количество подряд идущих ошибок резервного копирования;
- `last_snapshot_end` — время завершения последнего успешного снимка;
- `last_snapshot_error` — текст последней ошибки;
- `last_snapshot_start` — время начала последнего завершённого резервного копирования;
- `last_snapshot_url` — адрес последнего успешно созданного снимка;
- `next_snapshot_start` — время следующего запуска;
- `snapshot_start` — время начала текущего резервного копирования;
- `snapshot_url` — адрес текущего создаваемого снимка.

## На что обращать внимание при эксплуатации

При регулярной эксплуатации автоматических снимков рекомендуется контролировать:

- время последнего успешного снимка;
- наличие подряд идущих ошибок;
- доступность целевого хранилища;
- соответствие политики хранения требованиям вашей организации;
- объём доступного места для `local`-хранилища;
- корректность доступа к бакету и сетевой связности для `aws-s3`.

Если снимки создаются, но не проверяются и не тестируются на восстановление, такую схему нельзя считать полноценной стратегией резервного копирования.

## Практические рекомендации

- для production-окружений по возможности используйте внешнее объектное хранилище вместо локального диска;
- храните резервные копии вне того же контура, который они защищают;
- периодически проверяйте не только наличие файлов, но и возможность чтения снимков через `stronghold operator raft snapshot inspect`;
- регулярно выполняйте тестовое восстановление на отдельном контуре;
- ограничьте доступ к конфигурациям автоматических снимков и к целевым хранилищам;
- при использовании `local` учитывайте, что active-узел кластера может измениться;
- документируйте, какая конфигурация автоматических снимков отвечает за какой контур или среду.

## Связанные разделы

Для полной стратегии резервного копирования и восстановления используйте также следующие разделы:

- [Резервное копирование](../Резервное копирование/)
- [Восстановление из резервной копии](../Восстановление из резервной копии/)

## Что дальше

После настройки автоматических снимков рекомендуется периодически проверять их состояние и дополнительно использовать раздел [Восстановление из резервной копии](../recovery/) для отработки процедуры восстановления.
